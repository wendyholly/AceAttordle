Commands:

$aa [WORD] to guess a word e.g. $aa TRUCY (case insensitive, assumes 5 letters)

$hello - prints "Hello World!", used for checking whether bot is up
$dm - opens a dm - FUTURE! implement when it tracks games by user